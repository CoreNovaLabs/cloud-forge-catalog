services:
  rallly:
    image: daveearley/rallly:3.10.2
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/rallly:/app/data

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

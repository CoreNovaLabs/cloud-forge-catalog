services:
  filebrowser:
    image: filebrowser/filebrowser:v2.33.1
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/filebrowser:/srv

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

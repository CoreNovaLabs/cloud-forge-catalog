services:
  huginn:
    image: huginn/huginn:2.0.4
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/huginn:/var/lib/huginn

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

services:
  memos:
    image: neosmemo/memos:0.22.1
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/memos:/var/opt/memos

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

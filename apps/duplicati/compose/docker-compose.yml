services:
  duplicati:
    image: duplicati/duplicati:2.1.0.5
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/duplicati:/data

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

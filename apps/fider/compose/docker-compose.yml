services:
  fider:
    image: getfider/fider:0.24.0
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/fider:/app/.fider

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

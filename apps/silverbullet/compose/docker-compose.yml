services:
  silverbullet:
    image: silverbulletmd/silverbullet:0.10.1
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/silverbullet:/space

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

services:
  restic_rest_server:
    image: b26e/rest-server:0.13.0
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/restic-rest-server:/data

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

services:
  lobe_chat:
    image: lobehub/lobe-chat:1.54.3
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/lobe-chat:/app/data

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

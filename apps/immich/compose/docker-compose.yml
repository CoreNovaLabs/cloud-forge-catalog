services:
  immich:
    image: ghcr.io/immich-app/immich-server:v1.130.2
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/immich:/usr/src/app/upload

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

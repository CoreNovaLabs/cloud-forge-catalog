services:
  gitlab:
    image: gitlab/gitlab-ee:17.8.3-ee.0
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/gitlab:/var/opt/gitlab

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

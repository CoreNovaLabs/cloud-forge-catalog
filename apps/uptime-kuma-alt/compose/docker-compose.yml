services:
  uptime_kuma_alt:
    image: louislam/uptime-kuma:1.23.16
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/uptime-kuma-alt:/app/data

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

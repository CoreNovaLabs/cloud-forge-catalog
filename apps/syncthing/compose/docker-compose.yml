services:
  syncthing:
    image: syncthing/syncthing:1.29.7
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/syncthing:/var/syncthing

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

services:
  loki:
    image: grafana/loki:3.3.2
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/loki:/loki

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

services:
  uptime_kuma:
    image: louislam/uptime-kuma:1.21.3
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/uptime-kuma:/app/data

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

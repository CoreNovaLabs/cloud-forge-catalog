services:
  trilium:
    image: triliumnext/trilium:0.91.7
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/trilium:/home/node/trilium-data

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

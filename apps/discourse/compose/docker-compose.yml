services:
  discourse:
    image: discourse/discourse:3.4.0
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/discourse:/shared

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

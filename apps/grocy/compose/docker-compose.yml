services:
  grocy:
    image: lscr.io/linuxserver/grocy:4.4.2
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/grocy:/config

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge

services:
  gotify:
    image: gotify/server:2.6.1
    restart: unless-stopped




    volumes:
      - /opt/cloud-forge/data/gotify:/app/data

    networks:
      - cloud-forge

networks:
  cloud-forge:
    name: cloud-forge
